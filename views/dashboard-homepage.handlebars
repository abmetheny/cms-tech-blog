{{#each posts}}
<div class="card">
  <div class="card-header d-flex justify-content-between align-items-center">
    <a href="/dashboard/edit/{{id}}">
      <h4 class="my-1">{{title}}</h4>
    </a>
      <p class="m-0">Created on {{format_date date_created}}</p>
  </div>
</div>
{{/each}}

